#define WIN
#ifdef WIN // Windows compilation path

#include <WinSock2.h>
#include <WS2tcpip.h>

#else // linux compilation path

// TODO: Add Linux Compilation Path

#endif

#include <stdio.h>

int main()
{
    

    return(0);
}